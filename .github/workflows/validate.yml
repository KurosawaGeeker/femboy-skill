name: Validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  cases:
    name: Community cases
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate case metadata and index
        run: python3 scripts/validate_cases.py
